services:
  api:
    build:
      context: ./api
    environment:
      CATALOG_PATH: /app/data/catalog.sqlite
      PORT: "8080"
    volumes:
      - ./api/data:/app/data
    ports:
      - "18080:8080"
