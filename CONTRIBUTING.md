# Contributing

## Local setup

Install the pinned toolchain:

```console
$ mise trust
$ mise install
```

If you use asdf:

```console
$ asdf install
```

Install and run pre-commit hooks:

```console
$ pre-commit install
$ pre-commit run --all-files
```
